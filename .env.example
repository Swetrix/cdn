# Auth
ACCESS_TOKEN=SOME_SECRET_TOKEN

# Leave empty for all origins
API_ORIGINS=

# Maximum upload file size (in bytes); default is 10MB.
MAX_FILE_SIZE=

# File upload path. If not specified, the `cdn_files` folder will be created in the project's root directory.
UPLOAD_PATH=
